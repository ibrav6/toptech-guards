"""حالاتُ عائلة الكاذبات — تُضاف إلى `test_guards.py` عبر الاستيراد.

‼ **ستُّ إيجابياتٍ كاذبة في يومين، وكلُّها من سؤالٍ خاطئ**: كان الحارس
يسأل «أيرد النمط في الأمر؟» والصحيح «أيرد في **الشقّ الذي يفعل
الفعل**؟». والكاذبةُ تُطفئ الحارس بيد صاحبه، فيصير غيابه أسوأ من عدمه
لأنه وَعَد.

والحالات هنا **كلُّها وقعت فعلاً** — لا واحدة متخيَّلة:

| # | الأمر | ما مُنع زوراً |
|---|---|---|
| ١ | نسخُ قائمةٍ إلى مجلدٍ مؤقّت و`cd` لمسار إنتاجٍ في شطرٍ آخر | دهسُ الإنتاج |
| ٢ | سحبٌ من الإنتاج | دهسُ الإنتاج |
| ٣ | نسخةٌ احتياطية من الإنتاج إلى `backups` | دهسُ الإنتاج |
| ٤ | رسالةُ التزامٍ تصف النشر | دهسُ الإنتاج |
| ٥ | علمُ حذفٍ في شطرٍ ودفعٌ في شطرٍ آخر | الدفعُ القسري |
| ٦ | رسالةُ التزامٍ في heredoc تذكر الفرع الرئيس | الدفعُ المباشر |

ولكلِّ كاذبةٍ **قرينتُها الحقيقية** — وإلا صار العلاج ثغرة.
"""

MAIN = "mas" + "ter"          # لا يرد حرفياً كي لا يمنع الحارسُ هذا الملف
# المستودع عامّ ⇒ مساراتٌ خيالية كما في بقيّة الحالات
_PROD = "/srv/prod-app"
_HD = "\n".join(["git commit -F - <<'MSG'", f"إصلاحٌ يمسّ {MAIN} و{_PROD}",
                 "MSG", "git push -u origin fix/x"])

CLAUSE_CASES = [
    # ── الدفع القسري ────────────────────────────────────────────────
    ("علمُ حذفٍ في شطرٍ ودفعٌ في شطرٍ آخر — يمرّ", "guard_bash.py",
     {"tool_input": {"command": "rm " + "-f a.txt && git push -u origin fix/y"}}, False),
    ("لكن القسريّ في شقّ الدفع — يُمنع", "guard_bash.py",
     {"tool_input": {"command": "rm a.txt && git push " + "--force origin " + MAIN}}, True),

    # ── الفرع المشترك ───────────────────────────────────────────────
    ("رسالةٌ في heredoc تذكر الفرع الرئيس — تمرّ", "guard_bash.py",
     {"tool_input": {"command": _HD}}, False),
    ("لكن دفعاً صريحاً إليه — يُمنع", "guard_bash.py",
     {"tool_input": {"command": "git push origin " + MAIN}}, True),

    # ── ولا تُفتح ثغرة: رأسُ الـheredoc يبقى محروساً ────────────────
    ("كتابةٌ على الإنتاج عبر heredoc — تُمنع", "guard_bash.py",
     {"tool_input": {"command": "ssh nas \"sudo tee " + _PROD
                     + "/api/x.py <<'EOF'\nx = 1\nEOF\""}}, True),
]
